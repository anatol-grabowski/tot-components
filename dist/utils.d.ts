/**
 * Generated by `make types` from declaration files under `src/utils/`.
 * Edit the source utility declarations, not this file.
 */

/* ==========================================================================
 * Utility: computeHash
 * Source: src/utils/computeHash.d.ts
 * ========================================================================== */

/** Computes a SHA-256 digest and returns lowercase hexadecimal text. */
export function computeHash(data: ArrayBuffer): Promise<string>

/* ==========================================================================
 * Utility: decompressGzip
 * Source: src/utils/decompressGzip.d.ts
 * ========================================================================== */

/**
 * Decompresses gzip-compressed data with the Compression Streams API.
 *
 * Throws when the host does not support `DecompressionStream`.
 */
export function decompressGzip(compressed: ArrayBuffer): Promise<ArrayBuffer>

/* ==========================================================================
 * Utility: downloadFile
 * Source: src/utils/downloadFile.d.ts
 * ========================================================================== */

/** Starts a browser download for in-memory data. */
export function downloadFile(
  data: BlobPart,
  filename: string,
  mimeType?: string,
): void

/* ==========================================================================
 * Utility: formatSize
 * Source: src/utils/formatSize.d.ts
 * ========================================================================== */

/** Formats a byte count with a compact B, KB, MB, or GB label. */
export function formatSize(bytes: number): string

/* ==========================================================================
 * Utility: getFlagIcon
 * Source: src/utils/getFlagIcon.d.ts
 * ========================================================================== */

/**
 * Returns the regional-indicator flag for a locale containing an alpha country
 * code, or `null` when the locale has no such region.
 */
export function getFlagIcon(localeCode: string | null | undefined): string | null

/* ==========================================================================
 * Utility: hostEnvironment
 * Source: src/utils/hostEnvironment.d.ts
 * ========================================================================== */

/** Browser, platform, and application-host flags. */
export interface HostEnvironment {
  readonly isBrowserExtension: boolean
  readonly isElectronApplication: boolean
  readonly isWebBrowser: boolean
  readonly isDesktop: boolean
  readonly isChrome: boolean
  readonly isMsEdge: boolean
  readonly isFirefox: boolean
  readonly isChromiumBrowser: boolean
  readonly isMac: boolean
  readonly isWindows: boolean
  readonly isLinux: boolean
  readonly isMobile: boolean
  readonly isNativeApp: boolean
  readonly isAndroid: boolean
  readonly isIos: boolean
}

/** Current host-environment flags captured when the module is loaded. */
export const host: HostEnvironment

/* ==========================================================================
 * Utility: normalizeString
 * Source: src/utils/normalizeString.d.ts
 * ========================================================================== */

/** Converts text to a lowercase, diacritic-insensitive representation. */
export function normalizeString(value: string): string

/** Removes punctuation and whitespace from text. */
export function dropPunctuation(value: string): string

/* ==========================================================================
 * Utility: setSelection
 * Source: src/utils/setSelection.d.ts
 * ========================================================================== */

/**
 * Selects a text range inside an element.
 *
 * Offsets are measured across all descendant text nodes. `-1` means the end
 * of the element text.
 */
export function setSelection(
  element: HTMLElement | null,
  start: number,
  end?: number,
): void
