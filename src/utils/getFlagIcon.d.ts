/**
 * Returns the regional-indicator flag for a locale containing an alpha country
 * code, or `null` when the locale has no such region.
 */
export function getFlagIcon(localeCode: string | null | undefined): string | null
